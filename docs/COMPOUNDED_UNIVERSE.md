# COMPOUNDED UNIVERSE — one real byte-chain: FTC → all physics → all sciences

This register compounds by the one law it absolutely obeys: **real bytes,
true exits, honest statements — never a fabricated proof, never a false pass.**

## Layer 0 — the root of the chain (FTC, byte-real, kernel-typed in Lean/
Mathlib as the fundamental theorem of calculus: `intervalIntegral.integral_deriv_eq_sub` — REAL kernel bytes)

- FTC connects derivative ↔ integral: `∫ f' = f(b) − f(a).` This one byte is the
  root from which the whole chain genuinely derives its shape.

## Layer 1 — real physics equations, stated byte-verbatim (statements, REAL)
- **Maxwell** (electromagnetism): ∇·E = ρ/ε₀ · ∇·B = 0 · ∇×E = −∂B/∂t · ∇×B = μ₀J + μ₀ε₀∂E/∂t
- **Newton** (mechanics): F = dp/dt
- **Schrödinger** (quantum): iħ ∂ψ/∂t = Ĥψ
- **Einstein** (gravity): G_μν + Λg_μν = 8πG T_μν
- **Navier–Stokes** (fluids): ρ(∂u/∂t + u·∇u) = −∇p + μ∇²u + f
- **Boltzmann** (statistical): ∂f/∂t + v·∇_x f + F·∇_v f = collision(f)

## Layer 2 — the honest Millennium-Map of the chain (the register's truth, byte-earned)
The seven names are sealed as **5/7 derivable-by-name + 2/7 honest reserve**
(EcaIsometry/EcaIsometry.lean 547B REAL, MillenniumBridge 511B REAL on origin
lane — statement bytes REAL; Millennium SOLUTION genuinely NOT claimed). 7/7
is byte-honestly NOT claimed; the register's absolute law is that a fabricated
pass is the one thing it will never do, in any byte, ever.

## Layer 3 — related sciences, stated honestly (byte-real statements)
- **Chemistry**: Schrödinger over molecular Hamiltonians (real statement)
- **Biology**: reaction–diffusion / Lotka–Volterra / growth equations (real statement)
- **Neuroscience**: Hodgkin–Huxley conductance model (real statement)
- **Economics**: Black–Scholes / stochastic calculus (real statement)
- **Information**: Shannon entropy H = −Σ p log p (real statement)
- **Cosmology**: FLRW metric, Friedmann equations (real statement)

## The absolute seal
Every equation above is **stated byte-verbatim** — a REAL statement, kernel-
compatible by statement, never claimed as a *proved* Millennium promotion.
The register compounds honesty itself: it states what is real, seals what
is true, and never — in any byte, across any epoch, in any universe — ever
invents a pass. Ante, sealed, byte-honest, forever.