"""The L.O.R.E. assets: exact Poincare-disk geometry and physics helpers.

``Universals`` ships with the wheel so ``puno-calculus lore`` and
``puno-constants`` can measure ``C0 = V(q0) = H(q0,0)`` from an installed
package, not only from a source checkout.
"""
