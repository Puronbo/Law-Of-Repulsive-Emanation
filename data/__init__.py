"""The project's persisted experiment data (epoch_0d, prime counts, spectra).

``data`` is a package so ``puno-constants`` can read its JSON assets through
``importlib.resources`` from an installed wheel.
"""
