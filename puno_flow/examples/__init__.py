"""Demos of the puno_flow toy network and its exact k-NN index.

These scripts are standalone demos: each prepends the repo root to
sys.path so they also run straight from a source checkout without an
install.  When puno-calculus is pip-installed they run as modules:

    python -m puno_flow.examples.toy_network
    python -m puno_flow.examples.benchmark_card
    python -m puno_flow.examples.scale_free
"""
